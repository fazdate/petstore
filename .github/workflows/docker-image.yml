name: CI/CD to Azure Container Registry

env:
  AZURE_CONTAINER_REGISTRY: fazdateacr.azurecr.io
  AZURE_CONTAINER_REGISTRY_USERNAME: fazdateacr
  IMAGE_NAME: petStore

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        folder: [petstoreapp, petstoreorderservice, petstorepetservice, petstoreproductservice]

    steps:
    - name: Check out source code
      uses: actions/checkout@v3

    - name: Set up Java 17
      uses: actions/setup-java@v2
      with:
        java-version: '17'
        distribution: 'adopt'

    - name: Run Maven build
      run: |
        cd ${{ matrix.folder }}
        mvn --batch-mode --update-snapshots verify

    - name: Upload Maven build artifact
      uses: actions/upload-artifact@v3
      with:
        name: artifact-${{ matrix.folder }}
        path: ${{ matrix.folder }}/target/${{ env.IMAGE_NAME }}-0.0.1-SNAPSHOT.jar

    - name: Log in to Azure Container Registry
      uses: azure/docker-login@v1
      with:
        login-server: ${{ env.AZURE_CONTAINER_REGISTRY }}
        username: ${{ env.AZURE_CONTAINER_REGISTRY_USERNAME }}
        password: ${{ secrets.WEBAPPSECRET }}

    - name: Build and push Docker image to Azure Container Registry
      run: |
        cd ${{ matrix.folder }}
        docker build . -t ${{ env.AZURE_CONTAINER_REGISTRY }}/${{ env.IMAGE_NAME }}:latest -t ${{env.AZURE_CONTAINER_REGISTRY}}/${{ env.IMAGE_NAME }}:${{ github.sha }}
        docker push ${{ env.AZURE_CONTAINER_REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
        docker push ${{ env.AZURE_CONTAINER_REGISTRY }}/${{ env.IMAGE_NAME }}:latest
